apiVersion: v1
kind: Service
metadata:
  name: caragence-app
  namespace: caragence
  labels:
    app: caragence
    component: backend
spec:
  selector:
    app: caragence
    component: backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
