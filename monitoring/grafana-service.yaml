apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: caragence
  labels:
    app: caragence
    component: grafana
spec:
  selector:
    app: caragence
    component: grafana
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030
      protocol: TCP
  type: NodePort
